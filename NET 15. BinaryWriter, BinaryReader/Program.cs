// BinaryWriter, BinaryReader

using System.Text;

string path = "BinaryFile.txt";

string txt = "Some text";
int numb = 125;
double value = 36.647;

//using FileStream file = new(path, FileMode.Create);
//using BinaryWriter bw = new(file, Encoding.Unicode);
//bw.Write(value);
//bw.Write(txt);
//bw.Write(numb);

//using FileStream file = new(path, FileMode.Open);
//using BinaryReader br = new(file, Encoding.Unicode);
//Console.WriteLine(br.ReadDouble());
//Console.WriteLine(br.ReadString());
//Console.WriteLine(br.ReadInt32());

